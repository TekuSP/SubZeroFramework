<UserControl
    x:Class="SubZeroFramework.Controls.DeviceCapabilities.PlatformFirmwareSectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:deviceCapabilitiesControls="using:SubZeroFramework.Controls.DeviceCapabilities"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!--  Mockup "simplicity" layout: plain heading + motherboard stat tiles, no card chrome.  -->
    <StackPanel Spacing="10">
        <TextBlock
            FontSize="16"
            FontWeight="SemiBold"
            Foreground="{StaticResource TextPrimaryBrush}"
            Text="Motherboard" />
        <deviceCapabilitiesControls:StatTilePanel>
            <deviceCapabilitiesControls:StatTileView
                IconKind="Factory"
                Label="Manufacturer"
                Value="{x:Bind ViewModel.Snapshot.Inventory.Motherboard.Manufacturer, FallbackValue=Unknown, TargetNullValue=Unknown, Mode=OneWay}" />
            <deviceCapabilitiesControls:StatTileView
                IconKind="PackageVariant"
                Label="Product"
                Value="{x:Bind ViewModel.Snapshot.Inventory.Motherboard.Product, FallbackValue=Unknown, TargetNullValue=Unknown, Mode=OneWay}" />
            <deviceCapabilitiesControls:StatTileView
                IconKind="Pound"
                Label="Serial"
                Value="{x:Bind ViewModel.Snapshot.Inventory.Motherboard.SerialNumber, FallbackValue=Unknown, TargetNullValue=Unknown, Mode=OneWay}" />
        </deviceCapabilitiesControls:StatTilePanel>
    </StackPanel>
</UserControl>
