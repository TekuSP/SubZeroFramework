<Page
    x:Class="SubZeroFramework.Presentation.MenuItems.FanCurveProfiles.FanCurveProfilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cardModels="using:SubZeroFramework.Controls.FanCurveProfiles.Models"
    xmlns:controls="using:SubZeroFramework.Controls"
    xmlns:ctc="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fanControls="using:SubZeroFramework.Controls.Fans"
    xmlns:fanModels="using:SubZeroFramework.Controls.Fans.Models"
    xmlns:local="using:SubZeroFramework.Presentation.MenuItems.FanCurveProfiles"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:material="using:Material.Icons.UNO"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:uen="using:Uno.Extensions.Navigation.UI"
    xmlns:utu="using:Uno.Toolkit.UI"
    Background="{StaticResource AppBackgroundBrush}"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Grid
        x:Name="PageRoot"
        Padding="20,2,20,16"
        uen:Region.Attached="True"
        utu:SafeArea.Insets="VisibleBounds"
        RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Command feedback (validation warnings, service rejections, apply confirmations).
              PINNED at page level, deliberately OUTSIDE the master/detail scrollers: the model's
              ReportStatus previously wrote StatusMessage/IsStatusVisible that no view rendered at all,
              so "Apply all" with an invalid draft looked like the button did nothing (first reported on
              a one-fan Framework 13, where there is no other-fan activity to hint otherwise).  -->
        <muxc:InfoBar
            Grid.Row="0"
            IsClosable="True"
            IsOpen="{x:Bind ViewModel.IsStatusVisible, Mode=TwoWay}"
            Message="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
            Severity="{x:Bind ViewModel.StatusSeverity, Mode=OneWay}" />

        <!--  Master / detail  -->
        <Grid Grid.Row="1" ColumnSpacing="18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  MASTER: Fans by location  -->
            <local:FanListView Grid.Column="0" ViewModel="{x:Bind ViewModel, Mode=OneWay}" />

            <!--  DETAIL: editor  -->
            <local:FanDetailEditorView Grid.Column="1" ViewModel="{x:Bind ViewModel, Mode=OneWay}" />
        </Grid>
    </Grid>
</Page>

