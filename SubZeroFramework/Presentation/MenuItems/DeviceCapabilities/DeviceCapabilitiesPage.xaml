<Page
    x:Class="SubZeroFramework.Presentation.MenuItems.DeviceCapabilities.DeviceCapabilitiesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:deviceCapabilitiesControls="using:SubZeroFramework.Controls.DeviceCapabilities"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uen="using:Uno.Extensions.Navigation.UI"
    xmlns:utu="using:Uno.Toolkit.UI"
    Background="{StaticResource AppBackgroundBrush}"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Grid utu:SafeArea.Insets="VisibleBounds">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Padding="0,0,0,24" Spacing="18">
                <StackPanel Margin="15,0,15,0">
                    <TextBlock
                        FontSize="28"
                        FontWeight="SemiBold"
                        Foreground="{StaticResource TextPrimaryBrush}"
                        Text="Device Capabilities" />
                </StackPanel>

                <deviceCapabilitiesControls:OnboardStatusSectionView ViewModel="{x:Bind ViewModel.OnboardStatusSection, Mode=OneWay}" />
                <deviceCapabilitiesControls:SystemProfileSectionView ViewModel="{x:Bind ViewModel.SystemProfileSection, Mode=OneWay}" />
                <deviceCapabilitiesControls:CoolingSectionView ViewModel="{x:Bind ViewModel.CoolingSection, Mode=OneWay}" />
                <deviceCapabilitiesControls:CpuSectionView ViewModel="{x:Bind ViewModel.CpuSection, Mode=OneWay}" />
                <deviceCapabilitiesControls:StorageSectionView ViewModel="{x:Bind ViewModel.StorageSection, Mode=OneWay}" />
                <deviceCapabilitiesControls:MemorySectionView ViewModel="{x:Bind ViewModel.MemorySection, Mode=OneWay}" />
                <deviceCapabilitiesControls:PlatformFirmwareSectionView ViewModel="{x:Bind ViewModel.PlatformFirmwareSection, Mode=OneWay}" />
                <deviceCapabilitiesControls:GraphicsSectionView ViewModel="{x:Bind ViewModel.GraphicsSection, Mode=OneWay}" />
                <deviceCapabilitiesControls:NetworkSectionView ViewModel="{x:Bind ViewModel.NetworkSection, Mode=OneWay}" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
