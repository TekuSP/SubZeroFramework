<UserControl
    x:Class="SubZeroFramework.Presentation.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SubZeroFramework.Presentation"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:utu="using:Uno.Toolkit.UI"
    d:DesignHeight="300"
    d:DesignWidth="400"
    mc:Ignorable="d">
    <Border Background="{StaticResource BrandBackgroundBrush}">
        <utu:ExtendedSplashScreen
            x:Name="Splash"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            Background="{StaticResource BrandBackgroundBrush}">
            <utu:ExtendedSplashScreen.LoadingContent>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Image
                        x:Name="SplashLogo"
                        Grid.Row="0"
                        Margin="50"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Stretch="UniformToFill" />

                    <ProgressRing
                        Grid.Row="1"
                        Width="100"
                        Height="100"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        IsActive="True">
                        <ProgressRing.Resources>
                            <ResourceDictionary>
                                <ResourceDictionary.ThemeDictionaries>
                                    <ResourceDictionary x:Key="Default">
                                        <SolidColorBrush x:Key="ProgressRingBackgroundThemeBrush" Color="#D7D8FF" />
                                        <SolidColorBrush x:Key="ProgressRingForegroundThemeBrush" Color="#8F7FFF" />
                                    </ResourceDictionary>
                                    <ResourceDictionary x:Key="Light">
                                        <SolidColorBrush x:Key="ProgressRingBackgroundThemeBrush" Color="#D7D8FF" />
                                        <SolidColorBrush x:Key="ProgressRingForegroundThemeBrush" Color="#8F7FFF" />
                                    </ResourceDictionary>
                                    <ResourceDictionary x:Key="HighContrast">
                                        <SolidColorBrush x:Key="ProgressRingBackgroundThemeBrush" Color="#D7D8FF" />
                                        <SolidColorBrush x:Key="ProgressRingForegroundThemeBrush" Color="#8F7FFF" />
                                    </ResourceDictionary>
                                </ResourceDictionary.ThemeDictionaries>
                            </ResourceDictionary>
                        </ProgressRing.Resources>
                    </ProgressRing>
                </Grid>
            </utu:ExtendedSplashScreen.LoadingContent>
        </utu:ExtendedSplashScreen>
    </Border>
</UserControl>
